//var timeLeft = 60 * 60 * 1000;

function breakTime(){
	alert("Take a break");
};

var timeLeft = 6000;

setTimeout(breakTime, timeLeft);
// Don't put () next to function name or it will run it immediately

